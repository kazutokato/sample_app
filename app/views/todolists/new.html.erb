<h1>New Book</h1>

<%= form_with model:@list, url:'/todolists', local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <h4>Image</h4>
  <%= f.attachment_field :image %>
  <%= f.submit 'Create Book' %>
<% end %>